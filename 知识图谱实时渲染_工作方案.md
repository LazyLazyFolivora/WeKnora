# WeKnora 推理过程可视化 — 知识图谱实时渲染

## 工作方案汇报文档

---

## 一、项目背景与目标

### 1.1 问题描述

BioDSA agent 是一个生物医学领域的智能研究助手，运行一次需要 **3-5 分钟**。在这段等待时间内，用户在 WeKnora 对话界面只能看到一条空白的等待消息，**没有任何中间过程的反馈**，体验极差。

### 1.2 目标

在 WeKnora 对话界面的**消息卡片内**嵌入一个**实时生长的知识图谱**，让用户在等待期间能直观看到 agent 正在发现什么——新实体不断浮现、关系连线动态生长、研究进度一目了然。

### 1.3 预期效果

```
+-------------------------------------------+
|  对话界面                                  |
|                                             |
|  用户：帮我研究XXX药物的靶向机制...         |
|                                             |
|  药小知：                                   |
|  +---------------------------------------+  |
|  |  知识图谱（实时生长）                  |  |
|  |                                         |  |
|  |    [基因A]---靶向---[药物B]            |  |
|  |       |               |                |  |
|  |     关联           耐药                 |  |
|  |       |               |                |  |
|  |    [疾病C]         [突变D]             |  |
|  |                                         |  |
|  |  +------------+                         |  |
|  |  | 进度面板    | 实体：基因5 药物3      |  |
|  |  | 已用 2:30   | 阶段：深度挖掘中...    |  |
|  |  +------------+                         |  |
|  +---------------------------------------+  |
|                                             |
|  agent 文本回复（最终答案）                  |
+-------------------------------------------+
```

---

## 二、需求分析

### 2.1 功能需求清单

| 编号 | 功能模块 | 描述 | 复杂度 |
|------|----------|------|--------|
| F1 | 知识图谱实时生长 | 新节点浮现动画 + 边生长动画 + 矛盾发现闪烁 | 最高 |
| F2 | 研究阶段展示 | 广度检索/深度挖掘的视觉差异 + 阶段切换动画 | 中等 |
| F3 | 进度面板 | 当前阶段、已用时间、实体计数、发现时间线 | 中等 |
| F4 | 嵌入对话流 | 渲染在消息卡片内，不离开对话页面 | 较低 |
| F5 | 节点交互 | 悬停高亮、点击详情、缩放平移、小地图 | 高 |

### 2.2 验收标准

1. 收到第一个事件后 **1 秒内**出现对应的视觉变化
2. 节点浮现有**动画感**，不是瞬间全部出现
3. 同类型实体始终使用**同一种颜色**
4. 广度检索和深度挖掘阶段的**视觉感受有明显区别**
5. 进度面板的计数随事件**实时递增**
6. agent 结束后图谱**保持可交互**（缩放、点击），不消失不卡死
7. 整个过程**不离开** WeKnora 对话页面


## 三、现有架构分析


### 3.1 关键数据流（2026-08-15 更新）

```
后端 SSE 事件流
      |
      v
useStream.onmessage()           <- api/chat/streame.ts
      |
      v
useChatStreamHandler            <- composables/useChatStreamHandler.ts
  .processStreamChunk()
      |
      ├─ response_type="thinking/tool_call/answer/..." 
      │    → message.agentEventStream[] → AgentStreamDisplay.vue
      │
      └─ response_type="agent_graph"             ← 新增
           → window.dispatchEvent(CustomEvent('kg-sse-event'))
              → KnowledgeGraph.vue 监听
                 → useKnowledgeGraph.applyAgentGraphEvent()
                    → nodesMap / edgesMap 增量更新
                       → GraphCanvas.vue 重绘
      |
      v
botmsg.vue                      <- KnowledgeGraph 组件嵌入此处
```

### 3.3 消息卡片结构（botmsg.vue）（2026-08-15 更新）

当前 botmsg.vue 的渲染结构：

```html
<div class="bot_msg">
  <!-- Agent 模式 - RAG -->
  <div v-if="session.isRagMode" class="rag-answer-stack">
    <RagPipelineProgress />
    <AgentStreamDisplay v-if="session.isAgentMode" />
  </div>
  <!-- Agent 模式 - 非 RAG -->
  <template v-else>
    <docInfo v-if="session.knowledge_references?.length" />
    <AgentStreamDisplay v-if="session.isAgentMode" />
  </template>
  <!-- 知识图谱：v-show 保持组件不卸载，完成后仍可交互 -->
  <KnowledgeGraph v-show="session.isAgentMode || hasGraphData"
      :session-id="sessionId"
      :message-id="session.id"
      :agent-event-stream="session.agentEventStream"
      :is-completed="session.is_completed" />
  <!-- 文本回复 -->
  <div v-if="!session.hideContent && !session.isAgentMode">
    <div class="ai-markdown-template" v-stable-html="renderedHTML" />
  </div>
</div>
```

**关键设计决策**：
- 使用 `v-show` 而非 `v-if`，确保 Agent 完成后图谱不被卸载
- `hasGraphData` 计算属性检查 `agentEventStream` 中是否有 `agent_graph` 事件
- KnowledgeGraph 放在 RAG / 非 RAG 条件判断**之外**，两种模式均能渲染


### 3.4 已有知识图谱能力

项目后端**已有**知识图谱相关功能：

| 后端能力 | 前端对应组件 |
|----------|------------|
| `query_knowledge_graph` 工具 | `GraphQueryResults.vue`（已有的列表展示） |
| `graph_query` 工具 | `GraphQueryResults.vue` |
| `knowledge_graph_extract` 工具 | AgentStreamDisplay 中的工具卡片 |
| GraphSettings 配置 | `GraphSettings.vue`（知识库级别的图谱配置） |

**但现有能力仅限于列表形式展示图谱查询结果，没有可视化渲染。本次需求是新增实时动态渲染能力。**

---

## 四、技术方案

### 4.1 图谱渲染引擎选型（已确认）

经过调研和 Demo 验证，最终确认采用 **force-graph** 作为核心渲染引擎。

#### 选定 force-graph 的理由

1. **底层是 D3.js 生态**：使用 d3-force 做力导向布局、d3-zoom 做缩放平移，技术栈完全吻合
2. **Canvas 渲染，性能极强**：官方演示支持 **75,000 节点**，远超本项目需求（50-200节点）
性能数据来源：https://github.com/vasturiano/force-graph 
（**BioDSA agent 运行 3-5 分钟预计产生 50-200 个实体、100-400 条关系，在 force-graph 的能力范围内**）
3. **增量更新**：`graphData()` 支持动态添加节点和边，不需要全量重绘
4. **Vue 3 兼容**：Web Component 架构，天然兼容 Vue 3


### 4.2 新增依赖

```bash
pnpm add force-graph        # 力导向图渲染库（内含 d3-force, d3-zoom）(~178KB)
pnpm add -D @types/d3-force # d3-force TypeScript 类型定义
```

### 4.3 文件结构设计

```
frontend/src/
  types/
    knowledge-graph.ts              # 类型定义（新增）
  composables/
    useKnowledgeGraph.ts            # 图谱状态管理（新增）
  components/KnowledgeGraph/
    KnowledgeGraph.vue              # 主容器组件（新增）
    GraphCanvas.vue                 # force-graph 封装组件（新增）
    GraphProgressPanel.vue          # 进度面板（新增）
    GraphMinimap.vue                # 右下角小地图（新增）
    GraphNodeCard.vue               # 点击详情弹窗（新增，基于 TDesign Drawer）
    graph.less                      # 图谱样式（新增）
  views/chat/components/
    botmsg.vue                      # 修改：嵌入图谱组件
```

### 4.4 数据协议（2026-08-15 已与后端对齐）

后端已提供完整协议文档：`docs/api/agent-graph-frontend-protocol.md`

**SSE 事件结构**（`response_type = "agent_graph"`）：

```ts
// SSE data 层
AgentGraphSSEData {
  seq: number              // 单次 tool_call 内序号
  graph_event: string      // 事件类型
  tool_call_id: string
  payload: Record<string, unknown>
}

// 6 种 graph_event：
// EntitySearching  — upsert 节点 status=searching
// EntityConfirmed  — upsert 节点 status=confirmed, 写入 observations
// RelationFound    — upsert 边
// PhaseChange      — 更新 run.phase (broad_search | deep_dive)
// Progress         — 更新 step/total_steps/entities_found/relations_found
// RunComplete      — 标记完成，前端做全量 GET 对齐

// GET API：GET /api/v1/sessions/:sid/messages/:mid/graph?after_seq=0
// 返回：{ nodes[], edges[], run, last_seq }
```

**关键约束**：
- 节点 upsert key = `entity_name`（同 message 内唯一）
- 边 upsert key = `source_entity + target_entity + relation_type`
- 实体类型：gene, variant, drug, compound, disease, pathway, target, literature, finding, cell_line, tissue, unknown
- 关系类型为大写蛇形（如 ASSOCIATED_WITH, TREATS, INHIBITS）



### 4.5 开源功能复用分析（已验证）

经过调研，需求中的多个功能可直接复用 force-graph 的内置能力，无需从零开发：

| 需求功能 | force-graph 内置 | 需要自研 | 预估节省 |
|----------|-----------------|----------|----------|
| F1. 力导向布局 | ✅ 内置 d3-force | — | 2天 |
| F1. 节点渲染（颜色/标签） | ✅ `nodeColor` + `nodeLabel` + `nodeCanvasObject` | — | 1天 |
| F1. 边渲染（粗细/颜色/标签） | ✅ `linkWidth` + `linkColor` + `linkLabel` | — | 0.5天 |
| F5. 缩放平移 | ✅ 内置 d3-zoom | — | 1天 |
| F5. 节点拖拽 | ✅ 内置 drag | — | 0.5天 |
| F5. 节点悬停高亮 | ✅ `onNodeHover` + `nodeCanvasObject` | 略微定制 | 0.5天 |
| F5. 节点点击详情 | ✅ `onNodeClick` | 需写 TDesign 弹窗 | 0.5天 |
| F2. 研究阶段展示 | — | 需自研 | 1.5天 |
| F3. 进度面板 | — | 需自研（TDesign 组件） | 1天 |
| F1. 节点浮现动画 | — | 需自研（Canvas 自定义绘制） | 1天 |
| F1. 边生长动画 | — | 需自研（粒子效果可近似） | 1天 |
| F1. 矛盾发现闪烁 | ✅ `linkDirectionalParticles`（红色粒子） | 略微定制 | 0.3天 |
| F5. 小地图 | — | 需自研 | 1天 |

### 4.6 实体类型颜色映射

| 实体类型 | 颜色 | Hex |
|----------|------|-----|
| 基因 (gene) | 蓝色 | #3B82F6 |
| 药物 (drug) | 红色 | #EF4444 |
| 疾病 (disease) | 橙色 | #F97316 |
| 突变 (mutation) | 紫色 | #A855F7 |
| 通路 (pathway) | 绿色 | #22C55E |
| 化合物 (compound) | 青色 | #06B6D4 |
| 靶点 (target) | 深橙 | #EA580C |
| 文献 (literature) | 灰色 | #6B7280 |
---

## 五、可能遇到的难点与应对策略

### 5.1 技术难点

| 难点 | 风险等级 | 应对策略 |
|------|----------|----------|
| **force-graph API 学习** | 低 | 已完成 Demo 验证，API 简洁直观；团队可参考官方文档和 Demo 源码 |
| **实时渲染性能** | 低 | Canvas 渲染 + 增量更新，官方已验证 75k 节点无压力；我们的场景仅 50-200 节点 |
| **自定义动画（浮现/生长）** | 中 | force-graph 的 `nodeCanvasObject` 回调支持自定义绘制，需编写 Canvas 动画逻辑 |
| **小地图实现** | 中 | Canvas 缩放映射主视口；参考 D3 zoom+mini map 经典实现 |
| **和现有事件流集成** | 低 | 复用 agentEventStream 数组的响应式机制；在 processStreamChunk 中扩展事件类型 |
| **消息卡片宽度限制** | 低 | 图谱区域设定最小高度(300px) + 可折叠；响应式适配 |


---

## 六、团队分工（2026-08-10 更新）

### 6.1 团队组成

| 角色 | 人员 | 职责 |
|------|------|------|
| **组员A（组长）** | 成员A | 总体架构设计、核心模块开发、进度把控、功能完善后的 UI 美观优化 |
| **组员B** | 成员B | 进度面板 + 节点交互功能开发 |
| **组员C** | 成员C | 动画系统 + 小地图 + 样式适配 |

### 6.2 已完成任务

#### 组员A（组长）✅ 已完成

| 任务 | 内容 | 状态 | 说明 |
|------|------|------|------|
| 架构设计 | 文件结构、类型定义、数据协议 | ✅ 完成 | — |
| 核心类型 | types/knowledge-graph.ts | ✅ 完成 | 重写对齐后端协议，12种实体类型 |
| 状态管理 | useKnowledgeGraph.ts composable | ✅ 完成 | 去Mock，Map结构，SSE事件处理+GET API |
| force-graph 封装 | GraphCanvas.vue | ✅ 完成 | 性能优化：邻接缓存、rAF防抖、节点冻结防飘 |
| 主容器组件 | KnowledgeGraph.vue | ✅ 完成 | CustomEvent监听 + hasEverHadData持久化 |
| 集成到对话流 | botmsg.vue | ✅ 完成 | v-show持久化，RAG/非RAG均支持 |
| SSE事件路由 | useChatStreamHandler.ts | ✅ 完成 | agent_graph case + CustomEvent广播 |
| 性能优化 | GraphCanvas.vue | ✅ 完成 | Set邻接缓存、rAF防抖、节点冻结防飘 |
| 后端协议对接 | 全链路联调 | ✅ 完成 | SSE实时事件→图谱生长→完成后GET对齐 |

#### 组员B ✅ 已完成

| 任务 | 内容 | 状态 | 说明 |
|------|------|------|------|
| 进度面板 | GraphProgressPanel.vue（阶段/计时/计数/时间线） | ✅ 完成 | 实时更新 |
| 节点悬停 | 悬停高亮 + tooltip | ✅ 完成 | force-graph 内置 onNodeHover |
| 节点点击 | 点击详情弹窗 | ✅ 完成 | force-graph 内置 onNodeClick |
| 缩放平移 | — | ✅ 内置 | **force-graph 内置，无需开发** |

### 6.3 待分配任务

---

## 七、开发进度（2026-08-15 更新）

### 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| **F1. 知识图谱实时生长** | ✅ 核心完成 | 后端 SSE 事件实时驱动，Map 结构增量更新，防飘动冻结算法 |
| **F2. 研究阶段展示** | ✅ 完成 | broad_search / deep_dive 阶段标签切换，进度面板实时更新 |
| **F3. 进度面板** | ✅ 完成 | 阶段显示、计时器、实体计数、发现时间线、**可折叠** |
| **F4. 嵌入对话流** | ✅ 完成 | v-show 持久化，RAG/非RAG 模式均支持，完成后图谱保持可见 |
| **F5. 节点交互** | ✅ 完成 | 缩放平移、节点拖拽、悬停高亮（O(1)邻接缓存）、点击交互 |
| **后端 SSE 对接** | ✅ 完成 | `agent_graph` 事件路由 + CustomEvent 广播 + GET 全量对齐 |
| **性能优化** | ✅ 完成 | rAF 防抖、已有节点冻结防飘、邻接 Set 缓存、固定值替代回调 |
| **UI 优化** | ✅ 完成 | 屏幕空间恒定节点大小、缩放依赖标签显隐、力布局参数调优 |

### 已提交代码

```
commit c06772f7: feat: 知识图谱实时渲染 - 对话界面嵌入实时生长的知识图谱

基础文件（commit 23390911）：
- frontend/src/types/knowledge-graph.ts（类型定义）
- frontend/src/composables/useKnowledgeGraph.ts（状态管理）
- frontend/src/components/KnowledgeGraph/GraphCanvas.vue（force-graph 封装）
- frontend/src/components/KnowledgeGraph/GraphProgressPanel.vue（进度面板）
- frontend/src/components/KnowledgeGraph/KnowledgeGraph.vue（主容器）
- frontend/src/views/chat/components/botmsg.vue（嵌入知识图谱组件）
- frontend/package.json（添加 force-graph 依赖）

后端协议对接（2026-08-15 未提交的本地改动）：
- types/knowledge-graph.ts — 重写：对齐后端协议 12 种实体类型 + SSE/GET 类型
- useKnowledgeGraph.ts — 重写：去 Mock，Map 结构，applyAgentGraphEvent + fetchFullGraph
- useChatStreamHandler.ts — 新增 case 'agent_graph' + CustomEvent 广播
- botmsg.vue — v-show + hasGraphData，Agent 完成后图谱保持显示
- KnowledgeGraph.vue — CustomEvent 监听 + hasEverHadData
- GraphProgressPanel.vue — 可折叠面板
- GraphCanvas.vue — 性能优化：邻接缓存、rAF 防抖、节点冻结防飘
```

### 待完成

| 模块 | 状态 | 下一步 | 负责人 |
|------|------|--------|--------|
| **F1. 节点浮现动画** | ⏳ 待开发 | nodeCanvasObject 中实现 opacity + scale 渐入动画 | 组员C |
| **F1. 边生长动画** | ⏳ 待开发 | 粒子效果近似边生长 | 组员C |
| **F1. 矛盾发现效果** | ⏳ 待开发 | 红色虚线 + 闪烁（linkDirectionalParticles 已配置基础） | 组员C |
| **F2. 阶段切换过渡** | ⏳ 待开发 | 画布缩放 + 标签动画 | 组员C |
| **F5. 小地图** | ⏳ 待开发 | Canvas 缩放映射主视口 | 组员B |
| **节点点击详情弹窗** | ⏳ 待开发 | TDesign Drawer 展示实体详细信息 | 组员B |

### 已解决的技术难点

| 难点 | 解决方案 |
|------|----------|
| Vue prop 响应式不触发 watch | 改用 `window.dispatchEvent(CustomEvent)` 全局事件总线 |
| 图谱飘动 | `scheduleUpdate()` 中冻结已有节点 fx/fy，仅新节点参与力布局 |
| `isConnected()` O(n×m) 每帧调用 | 改用 `Set` 邻接缓存，O(1) 查找 |
| `graphData()` 高频调用 | `requestAnimationFrame` 防抖，同一帧只执行一次 |
| Agent 完成后图谱消失 | `v-show` + `hasGraphData` 替代 `v-if`，组件不卸载 |
| 节点缩放过小 | 所有尺寸 = `基准值 × (1/globalScale)`，屏幕空间恒定 |

### 技术验证结果（2026-08-15 更新）

| 验证项 | 结果 | 说明 |
|--------|------|------|
| force-graph 渲染能力 | ✅ 通过 | 官方支持 75k 节点，本场景 50-200 节点无压力 |
| Canvas 渲染性能 | ✅ 通过 | 比 SVG 性能更好，适合实时更新 |
| 力导向布局 | ✅ 通过 | charge -300 + link distance 200 效果良好 |
| Vue 3 响应式集成 | ✅ 通过 | CustomEvent 事件总线绕过 prop 响应式限制 |
| 节点拖拽交互 | ✅ 通过 | 力导向布局下拖拽正常工作 |
| 悬停高亮效果 | ✅ 通过 | O(1)邻接缓存，非关联元素变暗 |
| 后端 SSE 对接 | ✅ 通过 | agent_graph 事件实时驱动图谱生长 |
| 节点冻结防飘 | ✅ 通过 | 已有节点 fx/fy 冻结，新节点自然布局 |
| 屏幕空间恒定渲染 | ✅ 通过 | 所有尺寸 = 基准值 × (1/globalScale)，缩放自如 |

### 本次工作遗留问题（2026-08-15）

| 问题 | 描述 | 优先级 | 建议 |
|------|------|--------|------|
| **自定义渲染性能瓶颈** | `nodeCanvasObject` 每帧对每个节点执行 JS 回调（绘制 3 个圆 + 文字），100 节点 × 60fps = 6000 次/秒。官方 75k 节点测试用的是内置渲染，不走自定义回调 | 🔴 高 | 方案 1：去掉 `nodeCanvasObject`，改用内置渲染 + HTML overlay 做文字（性能最优但效果受限）；方案 2：离屏 Canvas 缓存，只在数据变化时重绘，主 Canvas 直接贴图 |
| **linkWidth/linkColor 回调已改为固定值** | 为性能已将 linkWidth(1.5) 和 linkColor 设为固定值，hover 高亮区分度降低 | 🟡 中 | 后续可考虑用离屏 Canvas 缓存 hover 状态下的渲染结果 |
| **节点浮现动画未实现** | 新节点直接出现，缺少 opacity + scale 渐入动画效果 | 🟡 中 | 在 `nodeCanvasObject` 中根据节点创建时间计算动画进度 |
| **边生长动画未实现** | 连线直接出现，缺少从起点到终点的生长效果 | 🟡 中 | 用 `linkDirectionalParticles` 粒子数/速度模拟 |
| **矛盾发现闪烁效果未完善** | 已配置红色粒子（`linkDirectionalParticles`），但缺少虚线 + 闪烁 | 🟡 中 | `linkLineDash` 可设虚线，闪烁用 `linkDirectionalParticleWidth` 周期变化 |
| **小地图未实现** | 用户在大图谱中定位不便 | 🟢 低 | Canvas 缩放映射主视口，参考 D3 zoom + mini map |
| **节点点击详情弹窗未实现** | 点击节点仅 console.log，无 UI 展示 | 🟢 低 | 用 TDesign Drawer 组件展示实体名称/类型/来源/描述 |
| **force-graph drag 与 Vue 响应式冲突** | force-graph 内部 drag 直接修改节点 x/y，可能与 Vue 的响应式系统产生冲突 | 🟢 低 | force-graph 已经处理了 drag，Vue 不应再监听节点坐标变化 |
| **代码未提交** | 本次所有后端协议对接和性能优化改动均在本地，未 commit | 🔴 高 | 下次开发前先 commit 当前改动，避免丢失 |

## 八、测试与其他

### 8.1 后端事件协议（2026-08-15 已确认 ✅）

后端协议已确认并实现，文档位于：`docs/api/agent-graph-frontend-protocol.md`

已完成对接：
1. ✅ SSE 事件类型 `agent_graph` — 已在 useChatStreamHandler 中路由
2. ✅ 6 种 graph_event payload 结构 — 已在 useKnowledgeGraph 中处理
3. ✅ GET API 全量对齐 — fetchFullGraph() 已实现
4. ✅ 实体类型和颜色映射 — 12 种类型已对齐

### 8.2 性能基准测试

- 50 个节点 + 80 条边的渲染性能
- 200 个节点 + 300 条边的压力测试
- 动画帧率监控 (requestAnimationFrame)

---


